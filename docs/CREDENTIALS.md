# Test Credentials for Stadium Copilot AI

The following accounts are available for testing the application on the live Vercel deployment. All passwords are `test123` except the admin which uses `admin123`.

| Role | Email | Password |
|------|-------|----------|
| **Admin / Organizer** | `admin@stadiumcopilot.com` | `admin123` |
| Organizer | `organizer@test.com` | `test123` |
| Fan | `fan@test.com` | `test123` |
| Volunteer | `volunteer@test.com` | `test123` |
| Staff | `staff@test.com` | `test123` |

These accounts are automatically seeded on app start when the Firebase configuration is missing (the mock fallback). They work both locally and on the live Vercel deployment.
